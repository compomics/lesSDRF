{
  "name": "lessdrf",
  "version": "1.0.0",
  "description": "This github repository contains the code used to create the lesSDRF application which is available via https://lessdrf.streamlit.app/\r A manual on the tool in pdf format is available here: https://github.com/compomics/lesSDRF/blob/main/lesSDRF_manual.pdf",
  "main": "main.js",
  "scripts": {
    "start": "electron-forge start",
    "build": "electron-builder",
    "build:mac": "electron-builder --mac",
    "build:win": "node scripts/build.js && electron-builder --win",
    "build:linux": "node scripts/build.js && electron-builder --linux",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@electron-forge/cli": "^7.7.0",
    "@electron-forge/maker-deb": "^7.7.0",
    "@electron-forge/maker-rpm": "^7",
    "@electron-forge/maker-squirrel": "^7.7.0",
    "@electron-forge/maker-zip": "^7.7.0",
    "@electron-forge/plugin-auto-unpack-natives": "^7.7.0",
    "chalk": "^4.1.2",
    "chokidar": "^3.6.0",
    "electron": "^35.0.0",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "tree-kill": "^1.2.2"
  }
}
